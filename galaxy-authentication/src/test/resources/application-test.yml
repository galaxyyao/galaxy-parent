spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:h2:mem:corm
    username: sa

auth:
  jwt:
    header: Authorization
    login-path: "/login"
    refresh-path: "/refresh"
    sso-path: "/sso/**"

